# TODO - Mock GUI (frontend only)

- [ ] Modify `Frontend/api_client.py` to support `MOCK_MODE` so Streamlit UI can render without Flask backend.
- [ ] Ensure mocked response matches keys used by `Frontend/ui.py` + `Frontend/components.py`.
- [ ] Run `streamlit run Frontend/ui.py` and confirm GUI works and displays demo results after clicking Analyze.

